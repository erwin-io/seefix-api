{
  "name": "seefix-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/agent-client.js",
    "test": "node --test"
  },
  "dependencies": {
    "@gradio/client": "2.5.1",
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "multer": "^2.0.0"
  }
}
